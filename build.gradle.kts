buildscript {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:7.3.1")
        classpath(kotlin("gradle-plugin", "1.7.20"))
        classpath("org.jetbrains.compose:compose-gradle-plugin:1.3.0-alpha01-dev849")
    }
}

group = "me.kevinschildhorn"
version = "1.0"

allprojects {
    repositories {
        google()
        mavenCentral()
        maven("https://maven.pkg.jetbrains.space/public/p/compose/dev")
    }
}